docker build -t dothattask_be .
docker buildx create --use
docker tag dothattask_be-web:latest <owner/dockerhubrepo>:latest

docker login

docker buildx build --platform linux/arm64 -t <owner/dockerhubrepo>:latest --push .
