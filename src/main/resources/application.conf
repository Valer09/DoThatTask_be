ktor {

    deployment {
        host = 127.0.0.1
        port = 8080
        port = ${?PORT}
    }

    application {
        modules = [ homeaq.dothattask.ApplicationKt.module ]
    }

    postgres {
        url = "jdbc:postgresql://192.168.2.105:5432/postgres"
        user = "admin"
        password = "Prevents6monoxide}"
    }
}