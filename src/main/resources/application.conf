ktor {

    deployment {
        host = 127.0.0.1
        port = 10000
        port = ${?PORT}
    }

    application {
        modules = [ homeaq.dothattask.ApplicationKt.module ]
    }

    postgres {
        url = "jdbc:postgresql://192.168.2.105:5432/dothattask_db"
        user = "admin"
        password = "Prevents6monoxide}"
    }

    database {
            embedded = true
        }
}